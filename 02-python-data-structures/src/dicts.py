my_dict = {}

# Add a key/value pair
my_dict['key'] = 'value'

# Update a value
my_dict['key'] = 'new value'

# Use an invalid key

# Test if a key exists
'key' in my_dict

# use .get()

# Delete a key
my_dict.pop('key')

# get the length of a dict
len(my_dict)

# Merge two dicts
my_dict.update({'x': 'y'})

# copy a dict
my_dict.copy()

# loop over a dict
for key in my_dict:
    pass